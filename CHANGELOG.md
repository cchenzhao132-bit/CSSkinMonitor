# 更新日志 Changelog

本项目遵循 [语义化版本](https://semver.org/lang/zh-CN/)。版本号与桌面 exe 发行版对应。

## [7.0.0] - 2026-08-31

### 数据契约重构（基于三份独立代码审查：grok/kimi/qwen，任务书见桌面 gpt.md）

**P0 · 7 日涨跌时间窗口不再静默降级**
- 新增 `find7dAnchor`：按时间戳寻找「今天-7 天 ± 2 天」内的真实历史锚点，替代旧 `priceHistory[length-8]` 取数
- 短历史（0/1/3 天）与仅剩 15/45 天锚点的第三方回填条目：`chgAvail=false`，UI 如实显示「数据不足 / 7日锚点未积累到」，**禁止 45 日数据冒充 7 日**
- 有 `chgPrev`（第三方 7 日成交中位）的条目仍优先使用真实成交口径
- 引擎产物新增 `chgAvail` 字段，前端榜单行 / 详情页 / 收藏页统计全部按此过滤

**P0 · 涨跌阈值统一为单一配置**
- 新增 `CHANGE_THRESHOLDS = { rising: 0.15, noticeable: 3, strong: 10, win7d: 2 }`（engine.js），榜单门槛（±0.15%）、涨跌分类（3%/10%）全部引用；前端 CHG_CLASS 描述由 `window.__CHANGE_THRESHOLDS` 派生
- 删除散落的魔法数（0.15 / 3 / 10），回归测试断言改为引用统一配置

**P0 · 历史边界防护（空/单点/非法价格不再产生 NaN 或崩溃）**
- 详情页 `lowIdx/highIdx/pctBetween/volatility` 对空数组、单点历史、0 价、null、NaN、Infinity 全部返回 null 或安全值，UI 显示「—」
- `listPrice`（真实挂牌）/ `currentPrice`（图表末点）语义注释明确，全库断言两者均为有限数

**P0 · 测试体系改为断言业务不变量**
- 新增 `tests/contract-audit.js`（`npm run test:contract`）：7 日窗口 0/1/3/7/8/15/45 天用例、涨跌临界值 18 组 + 非法输入、历史边界防护、findVariant 索引等价性、全库价格/榜单自洽
- regression.js 榜单断言从「实现自洽」改为「业务规则」（门槛/分类区间/无锚点不带涨跌数字）

**P1 · 性能与资源生命周期**
- `findVariant` 建 `(family|wear|col) → item` Map 索引：O(n) 暴力扫描 → O(1) 查询（详情页磨损表渲染不再数十万次字符串比较）
- 榜单懒加载 IntersectionObserver 改为模块级保存，重建前 disconnect（只生不灭 → 有生有灭）
- 搜索建议改事件委托，快速打字不再累积 click 监听器（此前每次输入重建 DOM 后 forEach 绑定）

**P2/P3 · 小问题**
- 空关键词不再插入无意义 `<mark>` 高亮标签
- 汇率（USD→CNY 固定 7.25）注释与 UI 明确标注「固定参考汇率，非实时」
- 收藏页排序对无涨跌数据条目防护（null 不参与 NaN 比较）

### 测试
- `npm run test:contract` 新增；全量回归 61/61、fixture 回归 62/62、炼金审计、渲染级验证、契约审计（全量+fixture）全部通过

## [6.3.2] - 2026-08-30

### 修复
- **低流动性配方不再进最赚榜**：无成交数据且三方挂牌/成交量合计 <10 的产出标记「低流动性」（卖出价不可执行），最赚榜剔除并透明计数（「另有 N 个高名义收益配方因产出/输入低流动性被剔除」）；最赔榜保留全部（风险提示不分流动性）。雷达 Top1 由 +¥40,318 的幻影配方（Vertigo 天价低流动性产出）变为 +¥585 的常规流动性配方（Prisma Case）
- **输入侧供给校验**：配方某输入行的三方挂牌量少于所需件数 → 现实中凑不齐，同样标记不可执行并剔除出最赚榜
- **炼金定价改用真实挂牌价（listPrice）**：此前用 currentPrice（图表序列末点）——不足 8 天真实历史的条目含模拟随机游走扰动，与雷达产生 20%+ 口径漂移（UMP-45 被抬高 24%）。引擎 buildItem 新增 listPrice（= 爬取时真实挂牌；refOnly 为三方最低），alchPrice/alchSalePrice 全部切换，回归审计口径一致性检查由 2 处失败 → 0
- 模拟器联动：极值卡片「⚠ 产出低流动性（净收益不可当可执行收益）」警示；产出表价格来源列追加「⚠ 低流动性」；净收益卡标注「⚠ 产出低流动性」

### 新增
- data.js ref 字段嵌入流动性信号（spq=Skinport 挂牌数 / mcv=market.csgo 成交量 / wxc=Waxpeer 挂牌数）
- 回归审计：illiquid 判定正/反例抽检（66/51）+ 有 p7 条目不得标记低流动性断言

## [6.3.1] - 2026-08-30

### 修复
- **炼金产出 EV 改成交口径（抗天价挂牌）**：此前产出按单一挂牌价计 EV，低流动性收藏品的天价挂牌直接抬高 EV（雷达 +¥40k 配方的根源）。现产出卖出价优先用 **7 日成交中位价**（Skinport last_7_days.median，成交量≥3 流动性门槛，全库 2112 条），无成交数据时回退 **Steam 挂牌 + Skinport/market.csgo/Waxpeer 三方 min 的多源中位数**（单一来源虚高被中位吸收）。输入成本仍按挂牌价（买入即付最低挂牌）
- data.js 全量条目新增 `p7` 字段（7 日成交中位，需 Skinport 历史回填）；产出表「参考价/价格来源」列改为「卖出参考价 / 7日成交中位 · 多源中位」
- 雷达脚本 `outPriceOf` 同口径；雷达提示语更新

### 新增
- 回归审计新增：median 单元向量 + alchSalePrice 成交口径抽检（200 条 p7 条目逐一核对）

## [6.3.0] - 2026-08-30

### 修复（引擎规则级，联网对照 Valve 现行规则多来源核实）
- **磨损公式改为归一化算法（2024-10 Retakes 规则）**：旧版直接平均原始浮动——混合不同磨损范围皮肤时产出磨损预测全错（范围 0-0.4 的皮肤浮动 0.30，旧算 0.30、游戏实算归一化 0.75，旧「填充皮压浮动」策略在现行规则下反向抬浮动）。现每个输入先按自身范围映射 0-1 再平均；极值扫描的 DP 桶轴同步改为归一化值；主面板同时显示原始均值与归一化均值
- **移除 10:1「隐秘→金色」**：该合同在游戏中不存在（2025-10 规则：隐秘→刀手套仅 5:1），此前 UI/扫描器/雷达都在模拟幻影合同
- **StatTrak 5:1 合同排除手套产出**：ST 合同只出 ST 刀（ST 手套不存在），此前 ST 模式把手套按普通价混入产出池
- **纪念收藏包警示横幅**：Souvenir 皮肤不能作为汰换输入（Valve 规则）；模拟器按同收藏品普通（非纪念品）版本计价，页面显著提示，防用户误买纪念品

### 变更
- 雷达脚本同步：归一化 DP、剔除 10:1 隐秘→金色扫描（组合 676→590）；极值卡片/雷达表头改标「归一化均值」

### 新增
- 回归审计新增归一化公式标准向量（take.skin / money4gamers 公开算例）+ 钳制/零范围防御 + avg∈[0,1] 不变量

## [6.2.3] - 2026-08-30

### 修复
- **「今日炼金雷达」界面丢失**：app.js 模块化拆分（81381cd）时雷达区块与其 CSS 未随迁移，`ALCHSCAN` 数据每日照常嵌入 data.js 但前端无人消费——已恢复雷达区块（最赚/最赔 Top 表、点击行载入模拟器）并补齐样式
- **雷达脚本 scan-tradeup.js 仍用段中点采样**：与前端 6.2.2 之前同款缺陷——成本随浮动连续下降，最优在磨损段高端，中点采样漏掉最优配方 → 改全桶扫描（扫描组合 604→676，能抓到此前漏掉的正收益配方）
- **雷达费率口径与模拟器不一致**：雷达对第三方参考价不扣 15% 费（偏乐观），模拟器统一扣 → 统一为 d337831 确定的官方口径（EV 全量 ÷(1+费率)）
- 雷达跳过单皮肤输入池（`pool.length < 2`）→ 与模拟器一致允许 10× 同款（≥1）
- 切换集合后当前档位可能不存在（纪念包缺档位/无金池）→ 自动回退到首个有效档位
- 手动模式中文名在磨损档选项、浮动区间钳制、切换手动预填价等 4 处仍未解析 → resolveN 统一处理
- 集合档位池为空时扫描提示误称「没有价格数据」→ 区分「该集合在此档位没有皮肤」

### 新增
- **炼金离线回归审计** `tests/alchemy-audit.js`（`npm run test:alchemy`）：池去重断言、alchOptimize 全量不变量（1680 组）、fmtSigned 符号、雷达与模拟器口径一致性逐条复算

## [6.2.2] - 2026-08-30

### 修复
- 炼金极值扫描「最赚/最赔」由磨损段中点采样改为**全桶扫描**：成本随浮动连续下降，最优在段高端，旧实现会把正收益最优配方整个漏掉（误报「全场无正收益」）
- 净收益符号 `¥-x` → `-¥x`（fmtSigned 负号前置）
- 炼金池同名涂装去重（Doppler 家族 7→1、Chroma2 金池 60→30，全库冗余 302→0），消除重复涂装虚高中奖概率
- 炼金主面板：**存在无价槽位时净收益/ROI 不再显示数字**——此前 10 件中 9 件无价时成本只计入 1 件，会显示虚高的假正收益（如 +¥143.23），与下方扫描器「全场无正收益」结论矛盾；现显示「—（N 件槽位无价，净收益不可信）」
- 期望产出 EV 在部分产出缺价时标注「（部分产出缺价·低估）」
- 手动模式（✎）槽位支持**中文名查价**：此前手动输入中文皮肤名无法匹配库内英文名，被计为无价（假正收益的另一诱因）
- 极值扫描标注无价变体数：「扫描 X 个（皮肤×磨损）变体 · Y 个无价变体未参与」

## [6.2.1] - 2026-08-30

### 修复
- 炼金极值公式扫描：产出存在缺价时按 ¥0 计入 EV（低估 EV、误判最赚/最赔）→ 该浮动段整体弃用，与离线扫描器 `scan-tradeup.js` 口径一致
- 炼金扫描无法可信估计时静默消失（如 StatTrak 模式遇老纪念包收藏品——全皮肤无 ST 版本，实测全市场 210 集合中 1046 个组合受影响）→ 显示原因提示
- 「最赚公式」在全场净收益为负时改为标注「亏得最少（全场无正收益）」，消除显示负数配方却称「最赚」的观感矛盾

## [6.2.0] - 2026-08-30

### 新增
- **炼金模拟器**（Trade-Up，2025-10 规则）：10:1 普通升级 + 5:1 隐秘→刀手套（集合金池 20% 权重混合）、产出浮动公式与磨损判定、EV/净收益/ROI、市场费率可调（默认 15%，官方口径按挂牌价 ÷ (1+费率)）、每槽 ✎ 手动输入皮肤与单价
- **今日炼金雷达**：`scan-tradeup.js` 每日扫描全市场 集合×档位 组合（当日快照价），最赚/最赔 Top 8，点击行载入模拟器细调
- **收藏系统**：全站 ☆ 星标（榜单/搜索/详情），收藏页统计合计价值与 7 日总涨跌，按 market_hash_name 持久化到 exe 同目录 `favorites.json`（pywebview JS 桥，重启不丢）
- **应用内行情刷新**：顶栏 ⟳ 手动刷新（30 分钟冷却，合规限流）+ 启动时数据落后 ≥2 小时自动后台刷新 + 完成热载入；爬虫支持 `CS_SKIN_HOME` 数据根目录（缓存/快照持久于 `%LOCALAPPDATA%\CSSkinMonitor`）
- **「无变动」榜**：置于涨价榜与降价榜之间，口径为涨跌榜精确补集（盘整 ±0.15% 内 + 涨跌积累中条目），按当前价排序；三榜守恒（涨+无变动+跌 = 全库）有回归断言
- **饰品中文名**：ByMykel zh-CN 官方本地化映射 16,098 条（8 类端点），列表/搜索建议/详情主显中文、英文保留为悬浮与副行；搜索中英文混合匹配
- **第三方条目图标补齐**：ByMykel 图标哈希 14,967 个，refOnly 武器类覆盖 96%（此前图片 URL 拼成 undefined 全 404）

### 变更
- **前端模块化**：1,342 行单文件 app.js → `app/js/` 7 个模块（core/router/views-list/fav/alchemy/detail/boot），file:// 限制下采用多 script 全局作用域方案
- **启动性能 3 倍**：引擎 90 天日期数组提升到模块级（消除 3.1 万件 × 90 天 ≈ 280 万次 Date 创建），启动构建 2.2s → 0.7s
- **data.js 瘦身 32%**：13.9MB → 9.5MB（去缩进、CDN 图片只存 icon 哈希由引擎拼前缀、去掉运行时无用的 imgKey）
- **图片四级加载**：捆绑本地图 → Steam 主 CDN → Akamai 备源 → SVG 剪影；AppData 与项目图片互相合并（897 张全命中）
- **数据目录统一**：应用内刷新、定时任务、命令行共用 `%LOCALAPPDATA%\CSSkinMonitor`，快照历史单一来源；每日定时任务新增雷达扫描步骤
- 涨跌榜覆盖全部 Steam 采集条目（不再限定热门池）；榜单 meta 显示「另有 N 件涨跌积累中」
- 深度爬取断点续传兼容数据目录迁移；市场费口径统一为「挂牌价 ÷ (1+费率)」

### 修复
- 第三方条目图标查找：完整市场名（含磨损后缀/ST/纪念品前缀）匹配不到基础名键 → 武器类 99.8% 缺图；改为逐级剥离的查找链（覆盖 96%，回归新增断言）
- 多源目录同步清空回填进度（histAt/hist 未保留）→ 同步时保留回填成果
- refOnly 条目 base 价格 NaN（Math.min 遇 undefined），影响 10,027 条显示 ¥0
- Skinport 429 惩罚窗口空转（静默跳过）→ 指数退避 10/20 分钟重试
- 涨跌金额双加号显示（列表行 + 详情价格卡）
- 应用内刷新弹黑色控制台窗口（CREATE_NO_WINDOW）

## [6.1.1] - 2026-08-30

### 修复
- **搜索功能完全失效**：模块化拆分（6.1 的 `app/js/` 重构）时搜索事件绑定整段丢失——输入框无监听、无下拉建议、无清空按钮，`state.query` 永远为空。已按原实现恢复（300ms 防抖 / 下拉建议 Top6 / 回车直达 / 点击外部收起），并新增 jsdom 端到端回归测试（Part 3，41→51 项）防止再发
- **应用内刷新后界面仍载入旧数据**：`_refresh_worker` 只更新 exe 同目录 `data.js`，而窗口渲染的 PyInstaller 解包目录副本仅在启动时覆盖一次，刷新完成后前端重载读到的仍是旧数据（须重启 exe）。现在刷新后同步覆盖运行时副本，重载即见新行情
- **数据引擎日期锚点硬编码** `2026-08-29`：每日重新生成数据后模拟段 90 天日期永久停在该日，与持续前进的真实快照日期错位。改为取生成时刻
- **炼金「极值公式扫描」费率口径**：净收益原用硬编码 13% 扣费，与主面板可调费率的官方口径（挂牌价 ÷ (1+费率)）不一致，费率非 13% 时两处对不上
- 中文别名搜索（如「爪子刀」）无命中高亮与定位（过滤与行渲染关键词不一致）
- 第三方参考价卡：无 Steam 价的条目全部被标红「高于 Steam」→ 无 Steam 价时不再标注高低
- 详情页返回按钮：从「无变动」榜进入时误显示「返回降价榜」
- 详情页图表 resize 监听器每次渲染都新增（长会话无限累积）→ 仅绑定一次
- 收藏页「7日合计涨跌」负数显示为 `¥-123.00` → 统一为 `-¥123.00`（fmtSigned）
- 页脚移除永久错误的硬编码「更新于 2026-08-29」
- 搜索命中定位 `scrollIntoView` 无存在性防护（无头/嵌入式环境抛未捕获异常）
- 删除死代码 `app/app.js`（模块化拆分残留，未被引用，此前仍被打进 exe）

## [6.1.0] - 2026-08-29

### 新增
- **多源数据**：接入 Skinport / market.csgo.com / Waxpeer 公开 API（`sources.js`），每轮各 1 次请求生成物品目录并集（`cache/catalog.json`）与覆盖率报告
- **目录并集入库**：Steam 未采集的约 2.3 万条目以三方最低参考价占位入库（`refOnly` 标记），全库 7,303 → 30,978 条（约 88% 市场覆盖），搜索可见、明确标注、不进涨跌榜
- **涨跌分类**：每件饰品按 7 日涨跌归类（大涨/上涨/盘整/下跌/大跌/无数据），榜单与搜索页分类筛选 chips，详情页分类标签
- **历史回填**：`--backfill N` 通过 Skinport 公开成交历史（24h/7d/30d/90d 窗口中位价，每批 100 名称、限流 40s）为第三方条目立即构建真实涨跌；7 天成交 <3 次的低流动性物品保持「无数据」
- **可选 Steam 历史层**：`--history N` 用本人会话 Cookie 调官方 pricehistory 端点（默认关闭）
- 详情页「第三方市场参考」卡：三源最低价与 Steam 挂牌价差百分比

### 变更
- 涨跌榜覆盖全部 Steam 采集条目（涨 3,382 / 跌 3,533），不再限定热门池；`hot` 仅作新鲜度标记
- 每日价格快照扩展到第三方参考价（`cache/price-history.json`，覆盖 2.4 万+ 条目），快照 ≥8 天自动转为真实涨跌，此前回退模拟（页面标注）
- exe 支持外置 `data.js`（同目录优先加载），刷新行情无需重打包

### 修复
- 快照裁剪误删第三方条目；`previousPrice` 短序列越界

## [6.0.0] - 2026-08-29

### 新增
- 磨损价位体系：详情页同皮肤家族各磨损真实挂牌价（普通/StatTrak™/纪念 三列），当前磨损高亮、价位条、点击跳转、版本切换
- 分类体系：8 类武器 + 印花/涂鸦/音乐盒/挂件/布章/探员/胶囊/武器箱，榜单 chips 筛选
- 全库搜索：中文别名映射，系列内所有磨损×版本按价格排序，行内实时涨幅
- 分层爬虫：热门层（~200 条/2 分钟）+ 深度层（weapons/knives/collect 全量断点续传）
- 开源化：README（含合规声明）/ MIT LICENSE / .gitignore / git 仓库

## [5.0.0] - 2026-08-29

- 磨损价位表（普通/StatTrak 双列）初版；榜单 8 类筛选；详情/行内分类标签
- `crawler.js --regen` 离线重建模式

## [4.0.0] - 2026-08-29

- 全市场真实数据：Steam 市场爬虫（Node + curl 绕 TLS 指纹、断点续传、自适应翻页），2,394 个饰品名称/价格/图片全真实
- 长列表 60 条/批 + IntersectionObserver 懒加载

## [3.0.0] - 2026-08-29

- Steam 市场真实饰品图片（本地图 + CDN + SVG 剪影兜底）

## [2.0.0] - 2026-08-29

- 浏览历史栈、顶栏返回按钮（层数徽标）、Alt+← 快捷键

## [1.0.0] - 2026-08-29

- 首个可用版本：暗色主题原型、涨跌榜、搜索、详情三价格卡 + ECharts 走势
- pywebview + PyInstaller 单文件 exe（~15MB）
